application.r is used to compute factors
application.m is the wrapfile for SIR, SAVE, DR, PHD method
select_main_sdrmethod.m is the code for specific sdrmethod with structure dimension selection. 
To implement select_main_sdrmethod.m requires a complete library for sufficient dimension reduction. The size of the library is too big to upload. We will put it in a public domain, such as github,  for sharing, 
